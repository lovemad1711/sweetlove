# 글자 표시 개선 완료

## 개선 내용

### 1. 전역 폰트 설정
- **기본 폰트 크기**: 10pt
- **한글 폰트**: 맑은 고딕 (Windows), NanumGothic (Linux), Apple SD Gothic Neo (macOS)
- **자동 폰트 감지**: 시스템에 맞는 폰트 자동 선택

### 2. 스타일시트 개선
- **모든 위젯**: 명확한 폰트 크기 설정
- **텍스트 색상**: 검정색(#000000) 명시적 지정
- **배경색**: 흰색 배경 명시적 지정
- **최소 높이**: 모든 입력 필드 25px 이상

### 3. 테이블 개선
- **텍스트 색상**: 검정색 강제 설정
- **행 높이**: 40-45px로 증가
- **패딩**: 8px로 증가
- **폰트**: 10pt 명시적 설정

### 4. 버튼 개선
- **최소 높이**: 25-35px
- **폰트 크기**: 10pt
- **텍스트**: 굵게, 흰색

### 5. UI 헬퍼 함수
새로운 `ui/ui_helpers.py` 파일:
- `create_table_item()`: 테이블 아이템 생성 (폰트, 색상 자동 설정)
- `get_standard_font()`: 표준 폰트 반환
- `set_row_heights()`: 행 높이 일괄 설정

## 수정된 파일

1. **main.py**
   - 전역 폰트 설정 추가
   - 스타일시트 대폭 개선
   - 모든 위젯 폰트 크기 명시

2. **ui/products_tab.py**
   - 헬퍼 함수 사용
   - 테이블 아이템 폰트/색상 명시
   - 행 높이 증가

3. **ui/ui_helpers.py** (신규)
   - 공통 UI 함수 제공

## 확인 방법

### 1. 프로그램 실행
```bash
python main.py
```

### 2. 품목 관리 탭 확인
- 품목 추가 버튼 클릭
- 입력 필드에 텍스트가 명확하게 보이는지 확인
- 저장 후 테이블에 데이터가 잘 보이는지 확인

### 3. 테스트 데이터 추가
```
품목명: 소주
박스당 수량: 20
단가: 1500
비고: 테스트
```

## 예상 결과

### 개선 전
- ❌ 텍스트가 너무 작거나 보이지 않음
- ❌ 입력 필드가 너무 작음
- ❌ 한글이 깨져 보임

### 개선 후
- ✅ 모든 텍스트 명확하게 표시 (10pt)
- ✅ 입력 필드 충분한 크기 (30-35px 높이)
- ✅ 테이블 행 높이 증가 (45px)
- ✅ 한글 완벽 표시
- ✅ 버튼 텍스트 잘 보임

## 추가 개선 가능 사항

### 글자가 여전히 작다면
`main.py`의 `main()` 함수에서 폰트 크기 조정:
```python
font.setPointSize(12)  # 10 → 12로 변경
```

### 행 높이를 더 높이고 싶다면
`ui/ui_helpers.py`의 `set_row_heights()` 사용 시:
```python
set_row_heights(self.table, 50)  # 45 → 50으로 변경
```

### 특정 OS에서 폰트가 안 보인다면
**Windows**: 폰트 설정 확인
```python
font.setFamily("맑은 고딕")
```

**Linux**: 한글 폰트 설치
```bash
sudo apt install fonts-nanum
```

**macOS**: 일반적으로 문제 없음

## 나머지 탭 개선

현재 `products_tab.py`만 완전히 개선되었습니다.  
다른 탭들도 같은 방식으로 개선 필요:

- [ ] initial_data_tab.py
- [ ] showcase_config_tab.py
- [ ] warehouse_tab.py
- [ ] outbound_tab.py
- [ ] showcase_tab.py
- [ ] sales_tab.py
- [ ] inventory_summary_tab.py
- [ ] reports_tab.py
- [ ] backups_tab.py

### 개선 방법
각 탭 파일에서:
1. `from .ui_helpers import create_table_item, get_standard_font, set_row_heights` 추가
2. 테이블 생성 시 `create_table_item()` 사용
3. `set_row_heights()` 호출

## 문의

글자가 여전히 보이지 않으면:
1. 운영체제 확인 (Windows/macOS/Linux)
2. 화면 해상도 확인
3. 시스템 DPI 설정 확인

필요시 추가 수정 요청하세요!
